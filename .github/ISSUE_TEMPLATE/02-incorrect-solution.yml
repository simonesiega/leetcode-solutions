name: Incorrect solution
description: Report a roadmap or company solution that fails or does not meet its requirements.
title: "[Bug] "
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a problem with a solution.

        Include a reproducible input and the expected output whenever possible.

  - type: input
    id: problem-number
    attributes:
      label: Problem ID
      description: Enter the numeric LeetCode ID or tracked OA problem ID.
      placeholder: "125 or oa-pairs"
    validations:
      required: true

  - type: input
    id: problem-title
    attributes:
      label: Problem title
      placeholder: Valid Palindrome
    validations:
      required: true

  - type: input
    id: solution-file
    attributes:
      label: Solution file
      placeholder: neetcode-all/twopointers/125.py or companies/roblox/solutions/oa-pairs.py
    validations:
      required: true

  - type: textarea
    id: failing-input
    attributes:
      label: Failing input
      description: Provide the smallest input that reproduces the problem.
      render: text
      placeholder: '"0P"'
    validations:
      required: true

  - type: textarea
    id: current-output
    attributes:
      label: Current output
      render: text
    validations:
      required: true

  - type: textarea
    id: expected-output
    attributes:
      label: Expected output
      render: text
    validations:
      required: true

  - type: textarea
    id: explanation
    attributes:
      label: What is wrong?
      description: Explain why the current result is incorrect.
    validations:
      required: true

  - type: textarea
    id: leetcode-details
    attributes:
      label: LeetCode error or additional details
      description: Paste the relevant error, failing test case, or submission details.
      render: text

  - type: checkboxes
    id: checks
    attributes:
      label: Before submitting
      options:
        - label: I verified that the issue is reproducible with the current repository version.
          required: true
        - label: I checked that a similar issue is not already open.
          required: true
        - label: I agree to follow the repository Code of Conduct.
          required: true
